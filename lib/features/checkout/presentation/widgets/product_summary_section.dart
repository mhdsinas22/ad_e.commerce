import 'package:flutter/material.dart';

class ProductSummarySection extends StatelessWidget {
  const ProductSummarySection({super.key});

  @override
  Widget build(BuildContext context) {
    // Hidden as per strict "UI ONLY" and matching images which don't show it.
    // If it needs to be shown, it can be implemented here.
    return SizedBox.shrink();
  }
}
